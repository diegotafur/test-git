0) git config --global init.defaultBranch main  : para por defecto dejar en main la rsama principal


1) git init : Sirve para inciaor el repositorio

2) git status -s : sirve para que esta pasando en el repositorio


3) git add . : con estos scanea todos los archivos 

4) git commit -m "primer comentario" : con este comando se hace el comit

5) git log --oneline : muestra el log


6) git config --global user.name "" : poner usuario 

7) git config --global user.email "" : 

8) git config user.name   : para ver le usuario

9*) git remote add origin https://github.com/diegotafur/test-git.git : voncula git con tu remoto

10*) git branch -M main   : con la rama configurada

ojo) git branch -m master main : para renombar rama 

11*) git push -u origin main : para subir

12) git push : directo cuando ya esta vinculado


// Para clonar un proeycto 

13) gti clone urlProject : sirve para clonar todo le proyecti 

//Ignorar archivos

14) git ignore : crear archivo .gitignore y dentro colocar los archivos que se van ignorar hast que se ponga gris


// Viajar en el tiempo con git 

15) git checkout  : si queremos visitar un commit en especifico captuar el id del commit

16) git revert :

17) git reset : vijar al commit objetivo , tomar ne cuenta que se peirde todos los commit




git push --set-upstream origin 01-rama